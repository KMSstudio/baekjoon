#include <iostream>
#include <algorithm>
#include <vector>

using namespace std;

void solve() {
    int year;
    cin >> year;
    cout << year + (1998 - 2541) << endl;
    return;
}

int main() {
    ios::sync_with_stdio(false);
    cin.tie(0);
    
    solve();

    return 0;
}